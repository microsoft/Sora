#pragma once

#if 0

namespace SoraDbgPlot { namespace Task {

	class TaskQueueRequester
	{
	public:
		TaskQueueRequester(size_t size)
		{
			_maxSize = size;
		}
	private:
		size_t _maxSize;
	};

}}

#endif
